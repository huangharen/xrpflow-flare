# XRPFlow｜100 项反驳式自审

> 每一行反驳一个可能出错的假设；最后一项明确保留必须由真实外部证据完成的事项。

001. 目标是 Flare Summer Signal。
002. 赛道是 Interoperable Asset Products。
003. 截止日期记录为 2026-08-14。
004. 项目名称是 XRPFlow。
005. 错误 HashKey 版本已独立归档。
006. 两个 Worker 地址均已覆盖为 Flare 页面。
007. 线上 HTML 不含 HashKey。
008. 线上 HTML 不含 HSP PAYMENT。
009. 线上 HTML 包含 Flare。
010. 线上 HTML 包含 XRPFlow。
011. 钱包网络固定 Coston2 114。
012. RPC 使用 Coston2 官方端点。
013. Explorer 使用 Coston2 浏览器。
014. Gas 代币标记 C2FLR。
015. 支付资产固定 FTestXRP 地址。
016. FTestXRP 明确标记无真实价值。
017. 演示不冒充主网 FXRP。
018. FTSOv2 通过 Registry 解析。
019. Oracle 地址不硬编码。
020. XRP/USD feed ID 使用 bytes21。
021. 零报价会拒绝。
022. 零时间戳会拒绝。
023. 未来时间戳会拒绝。
024. 超过五分钟的报价会拒绝。
025. FTSO 18 位精度处理明确。
026. USD 使用六位精度。
027. 部署验证代币六位精度。
028. 换算向上取整。
029. 界面建议 105% 储备。
030. 储备是上限而非收益承诺。
031. 价格下跌导致不足时不静默少付。
032. 允许付款方追加储备。
033. 过期后禁止追加储备。
034. 每笔负债独立托管。
035. 不存在模糊共享余额。
036. 测试验证全额担保。
037. 不存在管理员提款。
038. 不存在代理升级后门。
039. 创建者只能安排自己的资金。
040. 到期执行无需特权 Keeper。
041. dueAt 前禁止执行。
042. dueAt 边界允许执行。
043. expiresAt 后禁止支付。
044. 仅付款方可提前取消。
045. 第三方取消被测试拒绝。
046. 任何人可触发过期退款。
047. 退款始终返回原付款方。
048. 重复结算被阻止。
049. 取消后结算被阻止。
050. 终态重复操作被阻止。
051. 状态和会计先更新再转账。
052. 代币资金流有重入保护。
053. ERC20 调用使用 SafeERC20。
054. 链上只保存业务引用哈希。
055. 零收款地址被拒绝。
056. 零 USD 金额被拒绝。
057. 零储备被拒绝。
058. 到期与过期时间顺序被验证。
059. Math.mulDiv 避免中间溢出。
060. 多付款隔离有测试。
061. 取消会同步减少总托管。
062. 过期退款会同步减少总托管。
063. 结算和多余储备退款同交易完成。
064. 无钱包时明确提示。
065. 错误网络时要求切换 Coston2。
066. accountsChanged 会同步账户。
067. chainChanged 会同步网络。
068. 钱包拒签不会新增记录。
069. 交易回滚不会显示成功。
070. 未配置合约时明确显示 Prototype 模式。
071. 合约地址必须非零。
072. 配置地址必须存在字节码。
073. 前端核对 paymentToken。
074. 前端核对 Registry/FTSO。
075. 事件查询从部署区块开始。
076. 真实记录从链上事件恢复。
077. 真实交易链接 Explorer。
078. 演示交易使用 demo 前缀。
079. SSR 验证测试资产提示。
080. 演示记录不冒充真实交易。
081. 付款列表支持搜索。
082. 付款列表支持状态筛选。
083. 移动端有响应式导航。
084. 抽屉和对话框有可访问语义。
085. 按钮和输入有明确标签。
086. 外部链接使用安全 rel。
087. Git 忽略所有 Secret 环境文件。
088. 产品没有助记词输入框。
089. 产品没有私钥输入框。
090. Coston2 部署要求专用测试钱包。
091. Coston2 Explorer 已确认支持标准 JSON 和 solc 0.8.25 验证。
092. Solidity 生产构建成功。
093. 13 个合约测试全部通过。
094. 严格 TypeScript 通过。
095. ESLint 无错误。
096. vinext 生产构建通过。
097. 2 个 SSR 测试全部通过。
098. 真实链验收脚本覆盖创建、执行、取消、过期退款和最终完全托管检查。
099. 公开 GitHub 仓库、默认分支和线上主页已用 GitHub API 核验。
100. Coston2 合约、交易证据、视频和最终提交仍必须使用真实外部证据。
